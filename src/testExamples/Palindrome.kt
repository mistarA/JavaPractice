package testExamples

class Palindrome {

    fun main(args : Array<String>) {
        /*val value = "A man, a plan, a canal: Panama"
        var occurences = ArrayList<Char>()
        for (character : Char in value.toCharArray()) {
            if ((character >= 97 && character <= 122) || (character >= 65 && character <= 90) || ())
            if (occurences.contains(character)) {
                occurences.remove(character)
            } else {
                occurences.add(character)
            }
        }*/
    }
}

